query courses ($category: Int!) {
  courses (category: $category) {
    id: id
    title: title
    categories {
      id
      title
    }
    image: coverImage
    author {
      title
      profilePicture
    }
  }
}