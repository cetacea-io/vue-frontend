export default ({context, app, store, redirect}) => {
  if (!store.getters['authentication/funnelResponded']){
    // if (process.client){
      // app.loginModal.show()
      // app.loginModal.show()
    // }
  }
}