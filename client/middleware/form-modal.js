export default ({context, app, store, redirect}) => {
  if (!store.getters['auth/funnelResponded']){
    // if (process.client){
      // app.loginModal.show()
      // app.loginModal.show()
    // }
  }
}